create table dish(
                     id int primary key,
                     name varchar not null,
                     price int not null
);

insert into dish values (
                         1, 'Hot Dog', 15000
                        );